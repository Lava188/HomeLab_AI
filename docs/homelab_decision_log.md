# HomeLab Decision Log

| Decision | Context | Reason | Consequence | Status |
| --- | --- | --- | --- | --- |
| Build RAG-first before early fine-tuning | HomeLab needs grounded medical/lab answers and safety behavior. | KB/retrieval/eval can be inspected, tested, and corrected faster than fine-tuning at this stage. | Prioritize KB quality, retriever artifacts, runtime metadata, and smoke tests before model training. | Active |
| Expand KB by controlled batches | v1_3 added Batch A items such as sepsis, chest pain, breathing red flags, and blood-test result guidance. | Medical content must be reviewed and promoted deliberately. | Batch items carry protected runtime flags during packaging and require artifact/eval/runtime gates. | Active |
| Treat offline eval and runtime behavior separately | v1_3 offline FAISS eval is strong, but Node runtime does not use FAISS or `.npy` embeddings. | Offline success can overstate real product behavior. | Every release decision must check backend runtime metadata and user-facing smoke, not only artifact eval. | Active |
| Do not switch default/global runtime yet | Controlled semantic retrieval now works, but rollout is still gated. | Default promotion should wait for product review and the recommendation/test package prototype path. | Keep semantic retrieval controlled/opt-in; do not switch global defaults yet. | Active |
| Do not patch semantic failure with more keywords | Current router/retriever already contains many keyword, source-hint, and rewrite rules. | More keywords can hide the root semantic activation gap and make behavior brittle. | Next milestone must activate semantic retrieval, not add lexical camouflage. | Active |
| Keep rules for safety/guardrails, not as the main engine | Healthcare chatbot requires red-flag escalation and clarification gates. | Safety rules are appropriate for guardrails, but should not replace semantic understanding. | Rules remain for emergency, ambiguity, package gating, and low-confidence handling. Retrieval intelligence should move semantic. | Active |
| Use controlled semantic bridge/shadow mode next | Existing artifact has FAISS and `.npy`; backend Node could not originally consume them semantically. | Shadow mode can compare semantic top-k vs lexical top-k without risky user-facing promotion. | Implement bridge/service/subprocess or equivalent; expose semantic/lexical scores; keep fallback. | Completed for controlled bridge/retrieval |
| Chốt controlled semantic retrieval + safety-priority intent grouping before recommendation runtime | v1_3 bridge server and 8-case manual smoke passed. | Product needs truthful semantic retrieval and safe routing before package recommendations become live. | Do not switch everything to recommendation runtime yet; keep semantic retrieval controlled and use `intentGroup` for `urgent_health`, `test_advice`, `booking`, and `general_health`. | Active |
| Prioritize urgent_health over booking in mixed queries | Users may ask to book tests while also describing urgent red flags. | Safety escalation must not be suppressed by booking workflow. | Mixed urgent + booking queries route to `health_rag` with `intentGroup="urgent_health"`; normal explicit booking still routes to booking. | Active |
| Keep test_advice out of booking unless there is explicit booking/sample action | Users often ask what tests or packages are suitable before deciding to book. | Advice intent should ask for context and use RAG/policy, not start collection workflow. | `test_advice` stays in health RAG; booking requires clear booking/sample-collection action such as placing an appointment or requesting home sampling. | Active |
| Preserve fallback to stable retriever | v1_3 artifact/config can fail or be incompatible at runtime. | Health RAG should degrade gracefully instead of returning only artifact-read errors. | Use configured version plus fallback version metadata; current reports describe v1_2 fallback behavior. | Active |
| Ask clarifying questions for vague urgent queries | Eval v2 failure `v2_ambiguous_005` asks whether to go to hospital without concrete symptoms. | It is unsafe to answer a confident yes/no without context. | Router/policy should ask for symptoms and red flags, while escalating if serious signs are present. | Active |
| Route mixed customer test/infection questions through safety first | Eval v2 failure `v2_customer_need_003` mixes test selection and infection concern. | Package/test advice must not suppress possible infection/sepsis safety guidance. | Safety screening appears before test explanation; package-first answers remain blocked. | Active |
| Keep package recommendations gated | Reports indicate package catalog runtime remains disabled/gated. | Recommendation should only appear when safe, configured, and validated through the intended layer. | Do not infer product readiness from retriever smoke alone. | Active |
| Accept Recommendation Answer UX 3D | Recommendation Runtime 3B passed 10/10, 3C API metadata contract passed 9/9, and 3D answer UX smoke passed 7/7. | Safety priority remains intact, booking and urgent health are not interrupted by recommendation UX, and catalog disabled still keeps `recommendedPackage=null`. | Accept controlled slot-based recommendation/test package runtime prototype UX, but do not enable live package recommendation or package-first answers. | Active |
| Keep controlled live package recommendation behind a separate env gate | 3E flag-off regression passed 6/6, 3F frontend manual smoke passed 7/7, 3G catalog/source contract passed 6/6, and 3H controlled live package smoke passed 7/7. | Live package return should be possible only in controlled mode after runtime, safety, source, and catalog checks pass. It must not become default/global behavior by accident. | Use `HOMELAB_RECOMMENDATION_LIVE_PACKAGE_ENABLED=true` as the explicit live package gate. With the gate off, 3C/3D/3G behavior remains `recommendedPackage=null`; with runtime off, 3E behavior has no recommendation meta/UX/package IDs. | Active |
| Align recommendation sources with recommendation answer | 3F found source/provenance mismatch risk where recommendation answers could inherit unrelated RAG source chips. 3G fixed this for general/kidney recommendation answers, CBC boundary, and urgent health. | Source chips must support the answer the user sees; mismatched sources can make safe recommendation UX look medically inconsistent. | Recommendation/test-advice UX should hide or replace unrelated visible sources; CBC boundary can use CBC source; urgent health can keep suitable urgent/NHS sources. | Active |
| Treat package recommendation as controlled prototype only | 3H allows `recommendedPackage` to become non-null when runtime and live package gates are both enabled and enough safe context exists. | This validates controlled catalog-derived recommendation, not a complete intelligent recommendation engine or production default. | Do not hard-code package-first answers, do not weaken urgent/booking/medical-review/missing-context gates, and do not promote live recommendation without a future release decision. | Active |
| Accept KB/Retriever v1.4 Batch 4A as offline evidence for 4B controlled runtime candidate | Batch 4A completed source registry, raw capture, normalized/cleaned extraction, human review, approved KB dataset, merged corpus, embeddings/FAISS, eval v2, rerank experiments, and held-out v3. | Evidence is source-backed, human-reviewed, provenance-preserving, and held-out metrics are strong: held-out v3 Hit@3 0.8500, Hit@5 0.9000, MRR@5 0.6667 with warning/error 0. | Proceed to 4B controlled runtime integration of expanded-query + topic-aware rerank behind explicit flags. Do not promote v1.4 as default/global runtime yet. Freeze held-out v3 as evidence and avoid repeated tuning against it. | Active |
| Keep Retriever v1.4 offline until controlled runtime flags and smokes pass | At 4A close, v1.4 artifact validation passed with 97 chunks/vectors/FAISS ntotal and the best strategy was still offline-only. | Offline artifact/eval success can overstate real product behavior unless runtime wiring, metadata, fallback, and safety regressions are checked. | Completed by 4B-2G: controlled runtime flags, bridge/API wiring, flag-off, fallback, router, regression, and provenance smokes passed. Existing v1_3/default behavior remains safe when v1.4 flags are off; default/global promotion is still blocked pending broader frontend/manual observation. | Completed |
| Port Retriever v1.4 into runtime under controlled flags only | 4A offline evidence showed expanded-query + topic-aware rerank improves retrieval strongly, and 4B needed real API proof before any promotion. | Runtime integration must preserve RAG-first provenance, explicit observability, fallback, and safety gates while avoiding accidental default/global rollout. | Python bridge v1.4, Node semantic bridge, and real `/api/chat` health RAG path now support v1.4 only when explicit flags are enabled. Default/global runtime is unchanged; no frontend/package catalog/.env committed changes are required. | Active |
| Preserve urgent, booking, recommendation, flag-off, fallback, and provenance gates during v1.4 runtime integration | Controlled runtime retrieval can change routing and metadata if not guarded. | Healthcare safety and operational workflows must remain stable before broader rollout: urgent red flags beat test/recommendation/booking, booking/reschedule/cancel should not be hijacked, and bridge failures must not crash `/api/chat`. | 4B smoke matrix passed: bridge controlled 10/10, server contract 10/10 + health OK, Node controlled 10/10, router lab explanation 11/11, API controlled 9/9 + 2/2 gate, regression 14/14, flag-off 8/8, fallback 6/6, provenance 11/11. | Active |
| Force urgent_health answer policy before retrieval metadata differences | 4B-2H frontend/manual observation found strong red flags could route correctly but answer too softly if policy fell back to clarification, especially when v1.4 chunks use `topic`/`intended_use="emergency_warning"` rather than older `section`/`faq_type` metadata. | Safety UX must be stronger than retriever metadata format differences between v1.3/v1.4 or source chunk shape. | `urgent_health` now forces `primaryMode="emergency_or_urgent"` and `urgencyLevel="emergency"` for red flags such as chest pain + dyspnea + sweating; smoke 4B-2H passed 2/2 and 4B-2B/2D/2G regressions still pass. | Active |
| Do not infer booking testType from generic home sampling | Users can ask to book home sample collection without choosing a test yet. | Inferring "general blood test" from "lay mau tai nha" can create an unsafe or misleading booking draft. | Generic booking keeps `booking.draft.testType=null`, asks for the test type, and leaves `missingFields` including `testType`; booking/urgent/health_rag flows remain preserved. | Active |
| Keep raw source titles out of answer body while preserving provenance | 4B-2I found lab explanations could leak raw English source title/heading text such as "Is there anything else..." or "What are they used for?" into Vietnamese answer body. | Source title/domain/url belong in source metadata/source chips; body text should answer naturally without hiding provenance. | Answer body no longer inserts raw source title/heading; provenance remains in `meta`, citations, topChunks, and source metadata. Smoke 4B-2I passed 5/5 and 4B-2H/2B/2D/2G regressions still pass. | Active |
| Keep v1.4 controlled-only after basic frontend/API answer UX observation | 4B-2J manual UI + Network review after 4B-2H/2I found API behavior aligned and answer text more natural after minimal polish for lab explanations including ALT/AST, creatinine/eGFR, cholesterol/triglyceride, HbA1c, and HbA1c blood draw. | Basic manual UX observation plus smoke coverage is encouraging, but it is not enough evidence for default/global runtime promotion. Longer regression and broader manual observation are still needed. | v1.4 remains behind explicit controlled flags only. Do not promote default/global runtime; keep CBC boundary non-diagnostic, urgent red flags emergency-oriented, generic booking without inferred `testType`, and raw source headings out of answer bodies. | Active |
| Complete 4C product UX polish and source alignment without promotion | 4C addressed non-blocking UX/source findings after controlled observation hardening: pure lab explanation answers appended soft booking CTA, WBC-high could over-mention leukemia/blood cancer, urgent fever/confusion/rapid-breathing needed stronger source alignment away from D-dimer/generic blood-test drift, and NICE urgent-fever source chips could display `undefined`. | These were user-visible polish and metadata issues, not evidence for default/global runtime or live package promotion. | 4C smoke/regression and 7-case manual UI retest pass. Lab explanations stay educational, WBC-high stays neutral unless leukemia/blood cancer is asked, urgent fever remains emergency-oriented with NICE/sepsis-aligned source metadata, and source display names are valid. Retriever v1.4 remains controlled-only; live package recommendation remains controlled/not promoted. | Active |
| Accept 4D broader controlled regression evidence | 4D ran `backend/scripts/smoke_controlled_runtime_regression_4d.js` after 4C. | 29/29 passed across lab explanation, medical review/result boundary, urgent red flags, mixed urgent + booking/test advice, booking/reschedule/cancel, and recommendation/test advice controlled. This strengthens controlled demo/evaluation evidence without proving production readiness. | Accept 4D as broader controlled runtime regression/product review evidence. Keep retriever v1.4 controlled-only; do not promote default/global runtime; do not promote live package recommendation by default. Next step is thesis technical narrative packaging / khóa luận or future controlled observation, not immediate production/default release. | Active |
| Use MySQL/XAMPP + Prisma for booking runtime DB | 5B needed persistent booking, patient, staff, catalog, draft, slot, and status-history records. | MySQL/XAMPP matches the local development stack, and Prisma gives a typed schema/migration/client layer for the new business module. | Booking runtime uses Prisma provider `mysql` against `homelab_ai`; schema includes `Patient`, `TestCatalogItem`, `StaffProfile`, `AvailabilitySlot`, `Booking`, `BookingDraft`, and `BookingStatusHistory`. | Active |
| Create booking only after explicit user confirmation | Chat can collect all required booking fields in one message, but immediate creation could surprise users. | Booking is an operational/business action and should require a clear confirmation step. | Full booking info produces a confirmation summary first. Only messages such as "xac nhan", "dong y", or equivalent create a `CONFIRMED` booking and booking code. | Active |
| Require patientName and phone for confirmed booking | Operational staff need a named patient/contact, not only a test request. | Phone and patient name are minimum viable operational identity/contact fields for home sample collection. | Confirmed booking validation requires patientName, phone, test type or catalog item, sample date, sample time, and address. Missing fields remain in `BookingDraft`. | Active |
| Map test type to catalog when possible and fallback to testTypeText | User text may match a seeded catalog item, but not every request will map cleanly. | Catalog mapping supports structured operations while fallback preserves the user's requested test text without blocking booking. | Booking stores `testCatalogItemId` when matched; otherwise it stores `testTypeText`. Generic home sampling still does not infer a test type. | Active |
| Keep AvailabilitySlot capacity unenforced in 5B | `AvailabilitySlot` exists in schema, but capacity enforcement adds scheduling policy complexity. | The milestone goal is persistent booking workflow, not full scheduling optimization. | Capacity checks are future work; 5B booking creation does not reject based on slot capacity. | Active |
| Treat admin/staff API and dashboard as demo/internal tools without full auth/RBAC | 5B needed operational management but not production security. | Full auth/RBAC would expand scope beyond the professional prototype milestone. | `/api/admin/bookings` supports demo headers/default admin context, and `/admin/bookings` is an internal prototype dashboard. Real auth/RBAC is future work. | Active |
| Defer payment, SMS/email, and result upload | Product spec names these as out of scope for initial runtime. | They require separate product, security, and integration decisions. | 5B validates booking operations only. Payment, notification, and lab-result upload remain future modules. | Active |
| Preserve urgent_health priority over booking in 5B | Users may request booking while describing red-flag symptoms. | Safety-first behavior must not be weakened by the new booking business module. | Red-flag booking requests continue to route to urgent/emergency guidance and do not create confirmed bookings or new booking drafts. | Active |
| Use three role-specific booking UIs instead of one shared dashboard | 5C needed User/Patient, Admin, and Collector workflows on top of the 5B booking runtime. | The roles have different operational responsibilities and data visibility boundaries. | User/Patient, Admin, and Collector have separate dashboard routes and role-specific UI surfaces. | Active |
| Use separate login routes per role, not a shared `/login` | 5C is a role-based demo workflow. | Separate entry points make the intended role explicit and avoid a misleading generic auth surface. | Login routes are `/user/login`, `/admin/login`, and `/collector/login`; dashboards are `/user/dashboard`, `/admin/bookings`, and `/collector/dashboard`. | Active |
| Use demo localStorage auth and demo headers for prototype role routing | 5C needed role guards before production auth/RBAC exists. | This supports internal demo validation without over-scoping security work. | Frontend stores `homelab_demo_role`, `homelab_demo_token`, `homelab_demo_user_id`, and `homelab_demo_phone`; API clients send demo headers. This is not production auth/RBAC. | Active |
| Hide `internalNote` from User/Patient dashboard | 5C user dashboard exposes patient-facing booking detail. | Internal operational notes can contain admin/staff context that should not be shown to patients. | User booking APIs/UI return user-safe booking details and status history without `internalNote`. | Active |
| Scope Collector dashboard to assigned bookings only | Collectors should not see all system bookings or admin operations. | Collector workflow only needs assigned patient/time/address/test details and sample collection actions. | Collector APIs resolve `StaffProfile` by demo phone and only return bookings assigned to that collector; wrong collector phone gets controlled 404/no data. | Active |
| Keep Admin as the coordination role for booking operations | Admin needs broad operational visibility after 5B/5C. | Assignment, status progression, internal notes, and audit review are cross-booking coordination tasks. | `/admin/bookings` remains the all-booking operations dashboard with summary cards, workflow board, assignment, status update, internal note, and history. | Active |
| Keep payment out of 5C | Role-based booking operations do not require payment to validate the workflow. | Payment would require separate product/security/provider decisions and could distract from booking operations validation. | No payment UI/API was added; payment remains future work. | Active |
| Defer production auth/RBAC beyond 5C | 5C uses demo/internal role routing. | Real auth/RBAC needs a separate security design and should not be implied by localStorage demo auth. | 5C is explicitly not production-ready auth; future work may harden demo auth or implement real RBAC. | Active |
| Make status transition matrix and audit hardening the next backend focus | 5C validates role operations across status changes, but transitions remain broad demo operations. | A production-style operations workflow needs explicit allowed transitions, actor/audit consistency, and stronger rejection semantics. | Recommended next work is 5D-1 Status Transition Matrix + Audit Hardening, followed by 5D-2 Availability Slot/Capacity Enforcement. | Active |
| Use centralized booking transition matrix instead of free status updates | 5D-1 needed status workflow hardening after role-based operations were proven in 5C. | Broad status jumps such as `SAMPLE_COLLECTED -> COMPLETED` or terminal-state changes are unrealistic for lab operations. | `booking-status-transition.service.js` now defines the shared matrix used by admin, user, collector, and chat/runtime service paths. | Active |
| Treat `COMPLETED`, `CANCELLED`, and `NO_SHOW` as terminal booking states | Booking lifecycle should stop after completion, cancellation, or no-show. | Terminal states protect audit meaning and prevent accidental reopening through demo/admin APIs. | Transitions out of terminal states are rejected by the centralized matrix. | Active |
| Return controlled 409 for invalid booking status transitions | Invalid transitions are expected operational errors, not server crashes. | Clients and smoke tests need predictable failure semantics. | Invalid transitions return controlled JSON with `BOOKING_STATUS_TRANSITION_REJECTED`, HTTP 409, and a clear message such as `Không thể chuyển trạng thái từ COMPLETED sang ASSIGNED`. | Active |
| Keep audit schema unchanged in 5D-1 | Existing `BookingStatusHistory` already supports the needed prototype audit fields. | Schema/migration changes are unnecessary for this hardening slice and would expand scope. | 5D-1 writes `bookingId`, `fromStatus`, `toStatus`, `reason`, `changedByType`, `changedById`, and `metadata` using existing fields. Future audit schema expansion remains optional. | Active |
| Keep payment and production auth/RBAC out of 5D-1 | 5D-1 is workflow hardening, not commercial/security infrastructure. | Payment and production identity require separate product/security decisions. | No payment flow, production auth, or production RBAC was added; demo/internal role flow remains. | Active |
| Complete availability slot/capacity enforcement as backend hardening | Status workflow was centralized and smoke-tested in 5D-1. | The next largest operational gap was scheduling capacity and slot constraints. | 5D-2 now enforces opened slots and capacity. Recommended next work is 5E-1 Admin Availability Slot Management UI, then slot-aware booking UX/manual demo. | Active |
| Do not auto-create availability slots during booking creation | 5D-2 adds slot/capacity enforcement to the booking runtime. | Real operations should open capacity deliberately before users can book; auto-creating slots would hide scheduling gaps. | Chatbot/runtime booking rejects closed slots with controlled `BOOKING_SLOT_NOT_OPEN` instead of creating a slot implicitly. | Active |
| Use dynamic active booking count as the capacity source of truth | `AvailabilitySlot` has `bookedCount`, but prototype counters can become stale across cancel/reschedule/status changes. | Counting active bookings by `sampleDate + sampleTimeStart` keeps capacity behavior aligned with current booking state without schema changes. | Enforcement uses active booking count for capacity checks; `bookedCount` is not treated as the absolute authority in 5D-2. | Active |
| Treat active statuses as capacity holders and terminal statuses as released capacity | 5D-2 needed clear booking states for slot occupancy. | Confirmed and in-progress operations reserve collection/lab capacity; completed, cancelled, and no-show bookings should not block new booking capacity. | `CONFIRMED`, `RESCHEDULED`, `ASSIGNED`, `SAMPLE_COLLECTED`, `IN_LAB_PROCESSING`, and `RESULT_READY` hold capacity. `CANCELLED`, `NO_SHOW`, and `COMPLETED` release capacity. | Active |
| Return controlled errors for closed or full slots | Slot/capacity rejections are expected operational outcomes, not server failures. | Frontend/chat/manual demos need predictable messages and codes when users choose unavailable times. | Closed slots return `BOOKING_SLOT_NOT_OPEN`; full slots return `BOOKING_SLOT_FULL`; no confirmed booking is created in either case. | Active |
| Keep schema unchanged in 5D-2 | Existing `AvailabilitySlot` already has `date`, `startTime`, `endTime`, `capacity`, `bookedCount`, `area`, and `active`. | Schema/migration changes were unnecessary for the enforcement slice and would expand scope. | 5D-2 adds service/controller/route logic and smoke coverage without Prisma schema or migration changes. | Active |
| Add admin slot API before slot management UI | Capacity needs an internal API surface before a dedicated frontend management screen. | Backend smoke and future UI work need a stable route contract for listing and maintaining slots. | Added `GET`, `POST`, and `PATCH` under `/api/admin/availability-slots`; Admin Availability Slot Management UI is next work. | Active |
| Keep payment out of 5D-2 | Slot capacity is scheduling hardening, not commercial workflow. | Payment requires separate provider/security/product decisions and is not needed to validate capacity behavior. | No payment API/UI was added; payment remains out of scope. | Active |
| Add Staff Management UI/API for admin dispatch | 5F-1 needed clearer staff/collector operations after role UI, status workflow, and slot capacity were in place. | Admin needs a dedicated surface to manage staff profiles, active state, role, and workload visibility. | Added Admin Staff API and `/admin/staff` UI for the professional prototype operations layer. | Active |
| Prefer staff phone during assignment lookup | Staff names can overlap in demo or real operations. | Phone is a more specific operational identifier than name when the assign request includes it. | Assignment lookup prioritizes phone when available, reducing the risk of assigning the wrong staff profile. | Active |
| Reject inactive staff for new assignments | Inactive staff should not receive new collection work. | Dispatching to inactive staff creates unrealistic operations behavior and can hide availability issues. | Admin assign returns controlled `STAFF_INACTIVE_ASSIGNMENT_REJECTED`; staff can be reactivated before assignment. | Active |
| Restrict sample collection assignment to `SAMPLE_COLLECTOR` | Not every staff role should receive home sample collection bookings. | Role mismatch can confuse lab operations and collector dashboard visibility. | Admin assign returns controlled `STAFF_ROLE_ASSIGNMENT_REJECTED` when the staff role is not suitable. | Active |
| Reject assignment on terminal bookings | Completed, cancelled, and no-show bookings should not be reopened through assignment. | Terminal states protect lifecycle meaning and audit consistency. | Admin assignment rejects `COMPLETED`, `CANCELLED`, and `NO_SHOW` bookings with controlled response. | Active |
| Treat workload as visibility/warning, not route optimization | 5F-1 adds workload awareness but not mapping, route planning, or staff shift scheduling. | A lightweight prototype should show dispatch load without overclaiming workforce automation. | Staff API exposes workload summary and warning metadata; route optimization and notifications remain future work. | Active |
| Keep schema unchanged in 5F-1 | Existing `StaffProfile` already has the minimum fields needed for name, phone, role, and active state. | Schema/migration changes were unnecessary for this prototype hardening slice. | 5F-1 adds API/UI/rule logic without Prisma schema or migration changes. | Active |
| Keep route optimization, notifications, and production auth/RBAC as future work | Staff management improves operations visibility, but does not complete production workforce infrastructure. | These areas need separate product/security/integration decisions. | 5F-1 remains professional product prototype hardening; payment and production auth/RBAC stay out of scope. | Active |
| Require patient login/session phone for booking mutation | 5G-1 hardened booking create/reschedule/cancel behavior. | Booking mutation changes operational records and must be scoped to the user's session phone. | Booking mutation requires demo login/session phone; public health/lab questions and urgent red-flag guidance remain public. | Active |
| Require package selection/confirmation before vague booking creation | 5G-2 added package catalog booking behavior. | Vague requests such as "xét nghiệm máu" should not create a specific operational booking without choosing a package. | Booking stores selected `testCatalogItemId` and specific `testTypeText`; vague package booking asks the user to choose/confirm first. | Active |
| Let urgent red flags override package and booking pending state | 5G-2B preserved safety priority during package/booking flows. | Emergency symptoms must not be trapped inside package selection or booking confirmation. | Urgent red flags route to urgent guidance and do not create or continue booking/package state. | Active |
| Separate collector assignment lifecycle from BookingStatus | 5H added `CollectorAssignment` on top of booking operations. | Dispatch confirmation is a staff workflow and should not mark a booking assigned until a collector accepts. | Booking stays `CONFIRMED` while assignment is `PENDING_COLLECTOR_CONFIRMATION`; booking becomes `ASSIGNED` only after collector accept. | Active |
| Require reason and admin review for collector rejection | 5H-5/5H-6 added collector reject and admin review. | Rejection affects operations and needs audit context before reassignment. | Collector rejection saves `rejectReason`, sets `REJECTED_PENDING_ADMIN_REVIEW` and `reviewStatus=PENDING`; admin can approve or reject the rejection reason. | Active |
| Preserve assignment audit during manual reassign | 5H-6 added manual fallback after approved rejection. | Rewriting the old assignment would lose who rejected, why, and how admin reviewed it. | Manual reassign creates a new `CollectorAssignment` with source `ADMIN`; old rejected/approved assignment remains unchanged for audit. | Active |
| Keep payment and real notifications out of 5G/5H | 5G/5H focus on booking/package safety and collector assignment workflow. | Payment and SMS/email/realtime push require separate product/security/integration decisions. | No payment or real notification system was added in this phase. | Active |
| Avoid schema changes after 5H-1 through 5H-7 | 5H-1 introduced the collector assignment schema foundation. | Existing 5H-1 fields were enough for working profiles, matching, accept/reject, admin review, manual reassign, and E2E smoke. | 5H-2 through 5H-7 use existing schema; no additional Prisma migration was required. | Active |
| Commit GitHub only after meaningful functional behavior changes | Project has many small reports, scripts, and handoff docs. | Committing every small file churn makes history noisy. | Prefer commits after clear product/eval/runtime improvements or requested handoff milestones. | Active |
| Aim for professional prototype, not simple demo | Product goal is semantic healthcare chatbot with safety, retrieval, clarification, and recommendations. | Demo-like keyword responses will not meet the intended standard. | Technical work should improve real behavior, observability, and safety gates. | Active |

## Current Release Decision

HomeLab has completed stage 3 for the controlled slot-based recommendation/test package runtime prototype through 3H, KB/Retriever v1.4 Batch 4A through offline held-out validation in 4A-19, KB/Retriever v1.4 Batch 4B controlled runtime integration, 4C product UX/source alignment polish, 4D broader controlled runtime regression/product review, 5B Professional Booking & Lab Operations Runtime, 5C Role-based Booking Operations UI, 5D-1 Booking Status Transition Matrix + Audit Hardening, 5D-2 Availability Slot + Capacity Enforcement, 5F-1 Staff Management + Collector Workload Rules, 5G booking auth/package/urgent hardening, and 5H collector assignment workflow through full E2E smoke. The current state is **controlled recommendation prototype + controlled retriever v1.4 runtime path + professional booking/lab-operations product prototype with role-based internal/demo operations UI, centralized booking status workflow, slot capacity enforcement, staff workload visibility, package-confirmed booking, and collector assignment lifecycle**, not a full default-runtime switch, not default/global live package recommendation, not production auth/RBAC, and not production lab deployment.

The backend can use the persistent semantic bridge in health RAG with `selectedRetrievalMode="semantic_faiss"` when enabled, and manual frontend/runtime testing passed 8/8 for urgent health, test advice, normal booking, and mixed booking + urgent health priority. 4C manual UI retest passed 7 priority cases, and 4D passed 29/29 scripted cases across lab explanation, result boundary, urgent, mixed urgent/booking, booking/reschedule/cancel, and recommendation-controlled scenarios. Safety remains the priority: urgent health red flags beat booking actions, and test advice does not become booking unless the user clearly asks to book or collect a sample.

Recommendation Runtime 3B is accepted with 10/10 PASS and 3A regression 8/8 PASS. Recommendation API Metadata Contract 3C passed 9/9 with `testAdviceHasRecommendation=true`, `bookingUrgentNoRecommendation=true`, and `catalogDisabledKeepsPackageNull=true`. Recommendation Answer UX 3D passed 7/7. Flag-off regression 3E passed 6/6 after backend restart with recommendation runtime off. Frontend manual smoke 3F passed 7/7 with no raw package IDs and no booking/urgent interruption; newline/bullet flattening is a minor non-blocker. Catalog Contract + Recommendation Source Contract 3G passed 6/6 and fixed mismatched recommendation-visible sources. Controlled Live Package Recommendation 3H passed 7/7 with `HOMELAB_RECOMMENDATION_RUNTIME_ENABLED=true` and `HOMELAB_RECOMMENDATION_LIVE_PACKAGE_ENABLED=true`.

The next release decision should focus on whether and how to promote controlled package recommendation beyond explicit env-gated mode. Until then, live package recommendation remains controlled only: live gate off keeps `recommendedPackage=null`, runtime off removes recommendation meta/UX/package IDs, and safety gates still block live package return for `urgent_health`, booking, `medical_review_boundary`, and missing required context.

For retriever v1.4, the offline decision is positive for moving to 4B controlled runtime candidate:

- Batch 4A used 26 authoritative sources from `medlineplus.gov`, `nhs.uk`, and `niddk.nih.gov`.
- Human review produced 55 approved KB items, 58 revise, 15 reject, 0 pending.
- Approved QA passed 55/55 with warning/error 0, duplicate-like 0, suspected noise 0, and missing provenance 0.
- Merged corpus has 97 records: 42 legacy v1_3 chunks + 55 approved Batch 4A items.
- Embeddings/FAISS were built offline with `intfloat/multilingual-e5-small`, dimension 384, normalized embeddings, and `IndexFlatIP`; artifact validation has chunks/vectors/FAISS ntotal 97/97/97 with warning/error 0.
- 4A-18 expanded-query + topic-aware rerank reached Hit@1 0.6833, Hit@3 0.8333, Hit@5 0.8500, Hit@10 0.8833, Hit@20 0.8833, MRR@5 0.7589.
- 4A-19 held-out v3 reached total 40, Hit@1 0.4750, Hit@3 0.8500, Hit@5 0.9000, Hit@10 0.9250, Hit@20 0.9250, MRR@5 0.6667, warning/error 0.
- Held-out v3 failures are limited and categorized: `alias_gap_remaining=2`, `topic_missing_from_candidates=1`, `acceptable_broad_domain_but_wrong_topic=3`.

This supports the product thesis: natural Vietnamese question -> semantic understanding -> safety/urgent handling -> RAG retrieval -> grounded answer -> test/package recommendation path. It also reinforces the RAG-first decision: use provenance/auditability and human-reviewed medical knowledge before considering fine-tuning. Fine-tuning, if any, remains later and only after the RAG baseline is proven.

Retriever v1.4 controlled runtime decision:

- Decision: port retriever v1.4 into runtime under controlled flags only.
- Rationale: offline 4A showed expanded query + topic-aware rerank improved retrieval strongly; controlled runtime had to prove bridge/API/routing/fallback/provenance behavior before any promotion.
- Required flags: `HOMELAB_SEMANTIC_RETRIEVAL_ENABLED=true`, `HOMELAB_SEMANTIC_BRIDGE_MODE=server`, `HOMELAB_SEMANTIC_BRIDGE_URL=http://127.0.0.1:8766`, `HOMELAB_SEMANTIC_RETRIEVER_VERSION=v1_4`, and `HOMELAB_SEMANTIC_RETRIEVAL_STRATEGY=expanded_query_topic_aware_rerank`.
- Constraints: no default/global promotion, no frontend/package catalog/.env committed changes, preserve urgent/booking/recommendation gates, preserve fallback path.
- Outcome: controlled, flag-off, fallback, provenance, router, and API regression smokes all pass.
- UX polish outcome: 4B-2H urgent/booking smoke passed 2/2, with urgent-health red flags producing emergency/care-facility guidance and generic home-sampling booking no longer inferring `testType`; 4B-2I answer text polish passed 5/5, with source titles/headings kept in metadata/source chips rather than injected into answer body. Post-polish regressions remain green: 4B-2B 9/9 normal + 2/2 gate PASS, 4B-2D 14/14 PASS, and 4B-2G 11/11 PASS.
- Frontend/manual observation outcome: 4B-2J checked UI + Network for HbA1c, HbA1c blood draw, ALT/AST, creatinine/eGFR, cholesterol/triglyceride, CBC boundary, urgent red flags, and generic booking. Answers are more natural and still safety-bounded, but this remains controlled observation rather than a default/global promotion decision.
- 4C outcome: product UX/source alignment polish passed scripted and manual checks. Pure lab explanations no longer append soft booking CTA, WBC-high avoids leukemia/blood-cancer mention unless asked, urgent fever/confusion/rapid-breathing remains emergency-oriented, D-dimer/generic blood-test source drift is suppressed for the urgent fever cases, and the NICE source display no longer appears as `undefined - https://www.nice.org.uk/guidance/ng253`.
- 4D outcome: `smoke_controlled_runtime_regression_4d.js` passed 29/29. Coverage includes lab explanation, medical review/result boundary, urgent red flags, mixed urgent + booking/test advice, booking/reschedule/cancel, and recommendation/test advice controlled behavior. It also checks source-name validity, raw English source-heading leakage, raw package ID leakage, live-gate-off `recommendedPackage=null`, and D-dimer drift prevention for urgent fever/confusion/rapid-breathing.

Do not promote retriever v1.4 as default/global runtime yet. Do not promote live package recommendation by default. The next step is thesis technical narrative packaging / khóa luận or future controlled observation, not immediate production/default release.

5C role-based booking operations decision:

- Decision: accept the 5C role-based booking operations UI as an internal/demo workflow layer on top of 5B.
- Rationale: User/Patient, Admin, and Collector workflows have different visibility and operations boundaries.
- Outcome: user dashboard/API, collector dashboard/API, and polished admin operations dashboard are in place. 5C-2 user smoke passed 6/6, 5C-3 collector smoke passed, 5C-4 admin manual UI check was OK, and 5C-5 role-based E2E smoke passed 15/15.
- Constraints: demo localStorage auth and demo headers only; no production auth/RBAC claim; no payment; no RAG/retriever/recommendation promotion.
- Follow-up status: 5D-1 Status Transition Matrix + Audit Hardening, 5D-2 Availability Slot/Capacity Enforcement, slot management UX, and 5F-1 Staff Management + Collector Workload Rules are now complete. Next work is final operations polish/audit visibility and final regression/demo checklist.

5D-1 booking status transition/audit hardening decision:

- Decision: accept centralized booking status workflow enforcement as the backend source of truth for status changes.
- Rationale: role-based operations need consistent lifecycle rules across admin, user, collector, and chat/runtime paths.
- Outcome: transition smoke passed 12/12; 5C5 remained 15/15; 5C2 remained 6/6; 5C3 remained 6/6; 5B4 remained 9/9; 5B6 remained 12/12.
- Constraints: no Prisma schema/migration change; audit uses existing `BookingStatusHistory`; no payment; no production auth/RBAC; no RAG/retriever/recommendation promotion.
- Follow-up status: 5D-2 Availability Slot/Capacity Enforcement and 5F-1 Staff Management + Collector Workload Rules are now complete. Next work is 5G Final operations polish/audit visibility, then 5H Final product regression and demo checklist.

5D-2 availability slot/capacity enforcement decision:

- Decision: accept slot/capacity enforcement as backend scheduling hardening for the professional prototype.
- Rationale: booking creation and reschedule should not create operational commitments outside opened capacity.
- Outcome: 5D-2 slot capacity smoke passed 10/10; 5D-1 remained 12/12; 5C5 remained 15/15; 5C2 remained 6/6; 5C3 remained 6/6; 5B4 remained 9/9; 5B6 remained 12/12.
- Constraints: no Prisma schema/migration change; dynamic active booking count is the source of truth; no frontend slot management UI yet; no payment; no production auth/RBAC; no RAG/retriever/recommendation promotion.
- Recommended next work after later 5F-1 completion: 5G Final operations polish/audit visibility, then 5H Final product regression and demo checklist.

5F-1 staff management/workload decision:

- Decision: accept Staff Management + Collector Workload Rules as operations hardening for the professional prototype.
- Rationale: admin dispatch needs staff profile management, active-state visibility, role validation, and workload summary after slot/status/role flows are in place.
- Outcome: 5F-1 staff management workload smoke passed 9/9; 5E-3 full slot-role product demo remained 13/13; 5D-2 remained 10/10; 5C5 remained 15/15; frontend `npm run build` passed.
- Constraints: no Prisma schema/migration change; workload is visibility/warning rather than route optimization; no notification system; no payment; no production auth/RBAC; no RAG/retriever/recommendation promotion.
- Recommended next work: 5G Final operations polish/audit visibility, 5H Final product regression and demo checklist, then pause large feature work for thesis / khóa luận.

5G/5H booking and collector assignment decision:

- Decision: accept patient auth gate, package confirmation, urgent override, and collector assignment lifecycle as the current professional prototype workflow.
- Rationale: booking mutation needs session-phone ownership, vague package booking needs explicit package selection, and collector dispatch needs a separate acceptance/rejection lifecycle before booking becomes `ASSIGNED`.
- Outcome: 5G-1 passed 10/10, 5G-2 passed 8/8, 5H-1 passed 8/8, 5H-2 passed 10/10, 5H-3 passed 9/9, 5H-4 passed 8/8, 5H-5 passed 10/10, 5H-6 passed 10/10, and 5H-7 passed 12/12.
- Constraints: no payment, no real SMS/email/realtime notification, no route optimization, no production auth/JWT/RBAC, no RAG/retriever/recommendation promotion, and no schema changes after 5H-1 for 5H-2 through 5H-7.
- Recommended next work: documentation/thesis packaging and final demo polish unless a specific demo gap appears.

## Decision Criteria Before Revisiting Default Switch

| Criterion | Required state |
| --- | --- |
| Runtime semantic vectors/query embeddings | Active and same embedding space as artifact. |
| FAISS or equivalent vector search | Used directly or through a verified service/bridge. |
| Runtime metadata | Shows truthful mode, lexical score, semantic score, final score, loaded version, fallback state. |
| Semantic audit | PASS with non-zero semantic scores on relevant queries. |
| API smoke | PASS after semantic activation, not only lexical/rule wiring. |
| Frontend manual smoke | Completed and PASS. |
| Safety behavior | Emergency/urgent signals beat package/test suggestions. |
| Retriever v1.4 runtime candidate | Expanded-query + topic-aware rerank available behind explicit flags only; default remains unchanged. |
| Retriever v1.4 controlled runtime smoke matrix | PASS for bridge, server contract, Node service, API path, router, flag-off, fallback, regression, provenance, urgent/booking UX, and answer text polish. |
| Product UX/source alignment | 4C scripted smokes and targeted manual UI retest pass without default/global promotion. |
| Broader controlled regression | 4D scripted smoke passes 29/29 across A-F runtime/product groups without default/global promotion. |
| Held-out eval discipline | Held-out v3 frozen and not repeatedly tuned against. |

## Remaining Risks After Current Milestone

| Risk | Implication | Next action |
| --- | --- | --- |
| `test_advice` is a business-intent gate, not a full recommendation engine. | It can route and answer safely, but it does not yet choose personalized packages end to end. | Build a recommendation/test package runtime prototype. |
| Default/global runtime has not been switched. | Current semantic retrieval is controlled/opt-in rather than a broad production default. | Keep rollout gated until product review and additional smoke coverage. |
| Retriever v1.4 is controlled-only today. | 4B/4C/4D prove controlled backend runtime behavior and targeted answer/source UX improvements, but not default/global production readiness. | Keep v1.4 behind explicit flags; use current evidence for thesis/demo evaluation, not immediate production/default release. |
| Package recommendation UX is not complete. | Users may still need guided follow-up before package selection. | Design missing-context questions and package recommendation safety gates. |
| Controlled recommendation prototype is not default production recommendation. | 3H validates env-gated live package return, but only in controlled mode. | Do not promote default/global runtime or live package behavior without a future release decision. |
| Some test_advice rows may use lexical fallback. | 3C metadata contract is still valid, but semantic runtime coverage may need tightening later. | Track as non-blocker before stricter semantic coverage goals. |
| Live package output depends on flag state. | `recommendedPackage` can be non-null only when both runtime and live package gates are enabled and context is safe/sufficient. | Keep the three-state regression matrix visible: runtime on/live off, runtime off, runtime on/live on. |
| 5C demo auth is not production auth/RBAC. | localStorage role/session and demo headers are useful for prototype routing but not security. | Treat as internal demo only; future work must design production auth/RBAC separately if needed. |
| Booking status transitions need future policy refinement despite 5D-1 hardening. | 5D-1 centralizes the core lifecycle, but future product rules may need role-specific permissions, override policy, and richer audit semantics. | Keep the matrix centralized and extend deliberately if future workflows require exceptions. |
| Final operations/audit visibility still needs polish. | Role, slot, status, and staff operations exist, but final demo handoff may need cleaner audit/history visibility and checklist packaging. | Do 5G Final operations polish/audit visibility, then 5H Final product regression and demo checklist. |
| Staff workload is visibility/warning, not optimization. | 5F-1 shows assigned load and blocks inactive/wrong-role assignment, but does not optimize routes, shifts, service areas, or notifications. | Keep route optimization, notifications, and production workforce planning as future work. |
| 5G/5H validates workflow, not production operations. | Auth remains demo/session-header based; collector assignment has no real notification, route optimization, or payment integration. | Use 5H-7 as E2E-style prototype evidence and keep production auth/RBAC, notifications, payment, maps/routes, and lab-system integration as future decisions. |

## Decisions Needing Future Review

| Topic | Open question |
| --- | --- |
| Semantic architecture | Should the persistent Python bridge remain the long-term architecture, or be replaced by a more integrated vector service? |
| Runtime promotion | When should controlled semantic retrieval become the broader default? |
| Retriever v1.4 controlled integration | Controlled integration is implemented and smoke-tested; future review should decide if/when it becomes broader default. |
| Recommendation integration | How and when should package recommendation become live in full frontend/backend runtime? |
| Metadata UX | How much debug/citation metadata should be visible to users versus developers? |
| Live package recommendation promotion | Should controlled live package recommendation remain env-gated only, or be promoted after product/catalog/monitoring review? |
| Production auth/RBAC | Should demo localStorage role routing be replaced with real authentication, authorization, and staff/user identity management? |
| Booking transition policy expansion | Should future versions add role-specific overrides, mandatory reason policies, or richer immutable audit metadata beyond the 5D-1 matrix? |
| Slot management UX | How should admins create, edit, bulk open, and inspect slot usage in the frontend? |
| Staff route/workforce optimization | Should future versions add service area, collector availability windows, routing, shifts, notifications, or hard per-staff workload limits beyond the 5F-1 visibility/warning layer? |
